import random\nimport json\nfrom datetime import datetime, timedelta\n\n\ndef generate_sample_data(num_samples=100):\n    data = []\n    for _ in range(num_samples):\n        sample = {\n            'timestamp': (datetime.utcnow() - timedelta(days=random.randint(0, 30))).isoformat() + 'Z',\n            'temperature': random.uniform(-20, 5),  # degrees Celsius\n            'humidity': random.uniform(20, 100),  # percentage\n            'precipitation': random.uniform(0, 100),  # mm\n            'risk_level': random.choice(['low', 'medium', 'high'])\n        }\n        data.append(sample)\n\n    return data\n\n\ndef save_to_file(data, filename='avalanche_risk_data.json'):\n    with open(filename, 'w') as f:\n        json.dump(data, f, indent=4)\n\n\nif __name__ == '__main__':\n    sample_data = generate_sample_data()\n    save_to_file(sample_data)\n    print('Sample data generated and saved to avalanche_risk_data.json')\n