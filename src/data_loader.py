import pandas as pd\nimport torch\nfrom torch.utils.data import Dataset, DataLoader\n\nclass AvalancheRiskDataset(Dataset):\n    def __init__(self, csv_file):\n        self.data = pd.read_csv(csv_file)\n        self.features = self.preprocess_features(self.data)\n        self.labels = self.data['risk_label'].values\n\n    def preprocess_features(self, data):\n        data['temperature'] = (data['temperature'] - data['temperature'].mean()) / data['temperature'].std()\n        data['slope_angle'] = (data['slope_angle'] - data['slope_angle'].mean()) / data['slope_angle'].std()\n        data['snow_depth'] = (data['snow_depth'] - data['snow_depth'].mean()) / data['snow_depth'].std()\n        data['wind_speed'] = (data['wind_speed'] - data['wind_speed'].mean()) / data['wind_speed'].std()\n        return data[['temperature', 'slope_angle', 'snow_depth', 'wind_speed']].values\n\n    def __len__(self):\n        return len(self.data)\n\n    def __getitem__(self, idx):\n        return torch.tensor(self.features[idx], dtype=torch.float32), torch.tensor(self.labels[idx], dtype=torch.long)\n\n# Usage example\ndataset = AvalancheRiskDataset('avalanche_risk_data.csv')\ntrain_size = int(0.8 * len(dataset))\ntest_size = len(dataset) - train_size\ntrain_dataset, test_dataset = torch.utils.data.random_split(dataset, [train_size, test_size])\n\ntrain_loader = DataLoader(train_dataset, batch_size=32, shuffle=True)\ntest_loader = DataLoader(test_dataset, batch_size=32, shuffle=False)\n